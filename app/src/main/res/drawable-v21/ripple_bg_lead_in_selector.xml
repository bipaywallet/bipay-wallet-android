<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/main_head_bg_hover"
    android:radius="5dp">
    <!-- 点击后的背景色-->
    <item>
        <shape>
            <!-- 点击前的背景色-->
            <solid android:color="@color/transparent" />
            <corners android:radius="5dp" />
            <stroke
                android:width="1dp"
                android:color="@color/btn_option_normal" />
        </shape>
    </item>
</ripple>